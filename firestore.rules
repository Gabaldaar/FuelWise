/**
 * @fileoverview Firestore Security Rules for FuelWise application.
 *
 * Core Philosophy:
 * This ruleset allows any authenticated user to read and write data.
 * This is to support multi-user collaboration on the same set of vehicles.
 * Access is restricted to authenticated users only.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Allow any authenticated user to read and write anywhere.
    // This is a temporary diagnostic step.
    match /{document=**} {
      allow read, write: if isSignedIn();
    }

    // User profiles are still protected. This rule is more specific, so it takes precedence.
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }
  }
}